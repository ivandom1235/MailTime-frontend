# Copy to .env for local builds. On Vercel, set this in project Environment Variables.
# Public backend origin only: no trailing slash and no /api suffix.
VITE_API_BASE_URL=https://mailtime-1klh.onrender.com

# For a local backend, use http://localhost:5000 instead.
# Restart the dev server or rebuild/redeploy after changes.
# Keep the backend hostname in vercel.json's connect-src directive in sync.
# Never put database passwords, Microsoft secrets, or token caches in VITE_ variables.
